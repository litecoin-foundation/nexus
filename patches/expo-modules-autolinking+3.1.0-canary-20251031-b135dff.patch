diff --git a/node_modules/expo-modules-autolinking/scripts/android/autolinking_implementation.gradle b/node_modules/expo-modules-autolinking/scripts/android/autolinking_implementation.gradle
index f2279a6..860df17 100644
--- a/node_modules/expo-modules-autolinking/scripts/android/autolinking_implementation.gradle
+++ b/node_modules/expo-modules-autolinking/scripts/android/autolinking_implementation.gradle
@@ -156,28 +156,23 @@ class ExpoAutolinkingManager {
   }
 
   static void generatePackageList(Project project, Map options) {
-    String[] args = convertOptionsToCommandArgs('generate-package-list', options)
-
-    // Construct absolute path to generated package list.
+    // Workaround for canary version - skip node command and generate file directly in Gradle
     def generatedFilePath = getGeneratedFilePath(project)
+    def parentDir = new File(generatedFilePath).parentFile
+    parentDir.mkdirs()
 
-    args += [
-      '--namespace',
-      generatedPackageListNamespace,
-      '--target',
-      generatedFilePath
-    ]
+    // Generate Kotlin code since the file extension is .kt
+    def content = """package $generatedPackageListNamespace
 
-    if (options == null) {
-      // Options are provided only when settings.gradle was configured.
-      // If not or opted-out from autolinking, the generated list should be empty.
-      args += '--empty'
-    }
+import expo.modules.core.interfaces.Package
 
-    project.providers.exec {
-      workingDir(project.rootDir)
-      commandLine(args)
-    }.result.get().assertNormalExitValue()
+class ${generatedPackageListFilename.replace('.kt', '')} {
+  fun getPackageList(): List<Package> {
+    return emptyList()
+  }
+}
+"""
+    new File(generatedFilePath).text = content
   }
 
   static private String[] convertOptionsToCommandArgs(String command, Map options) {
